# ═══════════════════════════════════════════════════════════════════════════════
# Agentic Workflow — Environment Configuration Template
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env and fill in YOUR project-specific values.
#   cp .env.example .env
#
# Priority: .env  →  workflow-config.json  →  auto-detect  →  built-in defaults
# Any value left blank falls back to the next source in the chain.
# ═══════════════════════════════════════════════════════════════════════════════


# ── 1. Credentials ────────────────────────────────────────────────────────────
JIRA_EMAIL=
JIRA_API_TOKEN=
GITHUB_TOKEN=


# ── 2. Jira Project ──────────────────────────────────────────────────────────
JIRA_CLOUD_ID=
JIRA_BASE_URL=https://your-org.atlassian.net/
JIRA_PROJECT_KEY=
JIRA_PROJECT_NAME=
JIRA_PROJECT_ID=


# ── 3. Environment URLs ──────────────────────────────────────────────────────
UAT_URL=
DEV_URL=
INT_URL=
PROD_URL=


# ── 4. Backend Server ────────────────────────────────────────────────────────
SERVER_PORT=3100
CORS_ORIGINS=http://localhost:3001
NEXT_PUBLIC_BACKEND_URL=http://localhost:3100


# ── 5. Test Browser (Playwright tests) ───────────────────────────────────────
BROWSER_TYPE=chromium
HEADLESS=true
NAVIGATION_TIMEOUT=30000
TEST_TIMEOUT=120000


# ── 6. MCP Browser (live exploration) ────────────────────────────────────────
MCP_HEADLESS=true
MCP_BROWSER=chromium
MCP_TIMEOUT=60000
MCP_TOOL_TIMEOUT=120000
MCP_LOG_LEVEL=info


# ── 7. SDK Orchestrator ──────────────────────────────────────────────────────
COPILOT_MODEL=claude-sonnet-4-20250514
COPILOT_CLI_URL=
SDK_MAX_HEALING_ITERATIONS=3
SDK_PARALLEL_TICKETS=1
SDK_ENABLE_LEARNING=true


# ── 8. Reporting ──────────────────────────────────────────────────────────────
MODULE_NAME=
TEST_CYCLE=


# ── 9. CI / CD ───────────────────────────────────────────────────────────────
CI=false


# ── 10. Framework Overrides (optional — auto-detected by default) ─────────────
# FRAMEWORK_MODE=auto
# SPECS_DIR=
# PAGE_OBJECTS_DIR=
# CONFIG_DIR=
# TEST_DATA_FILE=
# BUSINESS_FUNCTIONS_DIR=
# UTILS_DIR=
# ENUMS_DIR=
