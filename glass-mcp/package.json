{
  "name": "glass-mcp",
  "version": "0.2.0",
  "private": true,
  "description": "Glass — a standalone, universal, lightweight browser MCP server. Affordance-first perception (see), durable content-addressed handles, a bounded verb algebra. Deterministic by default; cognition is opt-in and audited.",
  "type": "commonjs",
  "main": "src/index.js",
  "bin": {
    "glass-mcp": "src/server.js"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/run.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "ffmpeg-static": "^5.3.0"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0"
  },
  "optionalDependencies": {
    "playwright": "1.58.2"
  }
}
