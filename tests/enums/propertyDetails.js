/**
 * Enum for property features line
 * @readonly
 * @enum {string} PropertyFeaturesLine
 * @property {string} beds
 * @property {string} baths
 */
const PropertyFeaturesLine = {
    beds: 'Beds',
    baths: 'Baths',
    size: 'Size'
}

module.exports = {
    PropertyFeaturesLine
}
